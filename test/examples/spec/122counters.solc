// test multiple contract fields
import std.{*};
// import StorageLib;


contract Counter {
  counter1 : word;
  counter2 : uint256;
  counter3 : word;
  public function main() -> word {
    counter1 += 1;
    counter3 += 2;
    return counter1 + counter3;
  }
}
