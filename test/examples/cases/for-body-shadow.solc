import std.{Num,Add,Sub,Eq,Ord,Bounded,Typedef,le};

contract C {
    public function main() -> word {
        let x : word = 100;
        let i : word = 0;
        let s : word = 0;
        for(i=0;i<=0;i=i+1) { let x : word = 1; s = x; }
        return s;
    }
}
