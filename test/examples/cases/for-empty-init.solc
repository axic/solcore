import std.{Num,Add,Sub,Eq,Ord,Bounded,Typedef,le};

contract ForEmptyInit {
    function main() -> word {
        let i : word = 1;
        let s = 0;
        for(; i <= 10; i = i + 1) { s = s + i; }
        return s;
    }
}
