function inc(x : word) -> word {
  let f = lam () {
    let res : word ; 
    assembly {
      res := add(x,1)
    }
    return res;
  } ; 
  return f();
}

contract Foo {

  function main () -> word {
    return inc(0);
  }  
}
