import std.{Num,Add,Sub,Eq,Ord,Bounded,Typedef,le};

contract ForMultiPost {
    function main() -> word {
        let j = 0;
        for (let i = 0; i <= 3; i = i + 1, j = j + 2) {
            j = j + i;
        }
        return j;
    }
}
